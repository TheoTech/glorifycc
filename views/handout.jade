extends layout

block content
    each songs in songs2d
        -var i = 0
        -var j = 0
        -while (i < songs.length - 1 && j < songs.length - 1)
            table.table(style='text-align: center; page-break-after: always')
                thead
                    th(style='text-align: center') #{songs[i].title}
                    -i++
                    - while(i % 3 !== 0 && i < songs.length)
                        th(style='text-align: center') #{songs[i].title}
                        -i++
                tbody
                    tr
                        td
                            -for(var x = 0; x < songs[songs.length - 1]; x++)
                                - if (songs[j].lyric[x] === '')
                                    br
                                - else
                                    !{songs[j].lyric[x]}
                                    br
                        -j++
                        -while(j % 3 !== 0 && j < songs.length - 1)            
                            td
                                -for(var x = 0; x < songs[songs.length - 1]; x++)
                                    - if (songs[j].lyric[x] === '')
                                        br
                                    - else
                                        !{songs[j].lyric[x]}
                                        br
                            -j++
