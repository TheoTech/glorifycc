extends layout

block content
    include topnav
    h1 Select Translations
    #export1Table

    //- a.btn.btn-primary(href='/user/playlist/#{playlistName}/export3') Next

    script.
        var playlistID = !{JSON.stringify(playlistID).replace(/<\//g, "<\\/")}
        var playlistName = !{JSON.stringify(playlistName).replace(/<\//g, "<\\/")}
        var songs = !{JSON.stringify(songs).replace(/<\//g, "<\\/")}
        var translations2d = !{JSON.stringify(translations2d).replace(/<\//g, "<\\/")}
        var exportSongCollection = !{JSON.stringify(currentExportSongCollection).replace(/<\//g, "<\\/")}
    script(src='/dist/javascritps/export1Table.js')
    script.
        export1TableComponent.init(document.getElementById('export1Table'))
