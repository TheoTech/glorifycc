extends layout

block content
    include topnav
    h1 Preview
    each songs in songss
        -var i = 0
        -var j = 0
        -while (i < songs.length && j < songs.length)
            table.table(style='text-align: center; page-break-after: always')
                thead
                    th(style='text-align: center') #{songs[i].title}
                    -i++
                    - while(i % 3 !== 0 && i < songs.length)
                        th(style='text-align: center') #{songs[i].title}
                        -i++
                tbody
                    tr
                        td
                            -for(var x = 0; x < minLine; x++)
                                - if (songs[j].lyric[x] === '')
                                    br
                                - else
                                    !{songs[j].lyric[x]}
                                    br
                        -j++
                        -while(j % 3 !== 0 && j < songs.length)            
                            td
                                -for(var x = 0; x < minLine; x++)
                                    - if (songs[j].lyric[x] === '')
                                        br
                                    - else
                                        !{songs[j].lyric[x]}
                                        br
                            -j++
        //- -while (i < songs.length && j < songs.length)
        //-     table.table(style='text-align: center;')
        //-         thead
        //-             th(style='text-align: center') #{songs[i].title}
        //-             -i++
        //-             - while(i % 3 !== 0 && i < songs.length)
        //-                 th(style='text-align: center') #{songs[i].title}
        //-                 -i++
        //-         tbody
        //-             tr
        //-                 td
        //-                     each line in songs[j].lyric
        //-                         - if (line === '')
        //-                             br
        //-                         - else
        //-                             !{line}
        //-                             br
        //-                 -j++
        //-                 -while(j % 3 !== 0 && j < songs.length)            
        //-                     td
        //-                         each line in songs[j].lyric
        //-                             - if (line === '')
        //-                                 br
        //-                             - else
        //-                                 !{line}
        //-                                 br
        //-                     -j++
    
    form(method='post', role='form', action='')
        <button class="btn btn-primary btn-lg" role="button" aria-haspopup="true" aria-expanded="false">
        <span class="glyphicon glyphicon-download" aria-hidden="true"></span> Export </button>
