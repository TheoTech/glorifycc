extends layout

block content
    include topnav
    - var langCapitalized = song.lang.charAt(0).toUpperCase() + song.lang.slice(1)
    if messages[0]
        .alert.alert-danger
            each msg in messages
                p #{msg.msg}
        
    form(role='form', method="post", action="")
        .row
            .col-md-6
                h1 Original Song
            .col-md-6
                h1 Translation
                if hasErrors
                    .alert.alert-danger 
                        each msg in messages
                            p #{msg}      
        .row
            .col-md-6
                .form-group
                    label(for="lang") Language
                    input#language.form-control(name="lang", value=langCapitalized, readonly)         
                .form-group
                    label(for="title") Title
                    input#title.form-control(name="title", type="text", value=song.title, readonly)    
                .form-group
                    label.capitalize(for="copyright") Copyright
                    input.capitalize.form-control(name="copyright", value=song.copyright, readonly)
            .col-md-6
                .form-group
                    label(for="lang_t") Language
                    select#language.form-control(name="lang_t")
                        option(value="english") English
                        option(value="mandarin") Mandarin
                        option(value="spanish") Spanish
                        option(value="portuguese") Portuguese
                .form-group
                    label(for="title_t") Title
                    input#title.form-control(name="title_t", type="text")
                .form-group
                    label(for="copyright_t") Copyright
                    select#copyright.form-control(name="copyright_t")
                        option Public
                        option CC0
        .row
            .col-md-6
                .form-group
                    label(for="lyric") Lyric
                    textarea#lyric.form-control(name="lyric", rows='20' readonly) #{stringLyric}
            .col-md-6
                .form-group
                    label(for="lyric_t") Lyric
                    textarea#lyric.form-control(name="lyric_t", rows='20')
                button.btn.btn-primary(type="submit") Submit Translation
