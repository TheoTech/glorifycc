extends layout

block content
    include topnav
    h1 My Library
    br
    #library

    script(type='text/javascript').
        var s = !{JSON.stringify(songs).replace(/<\//g, "<\\/")}
        var playlists = !{JSON.stringify(playlistLibrary).replace(/<\//g, "<\\/")}
    script(src='/bower_components/mithril/mithril.min.js')
    script(src='/javascripts/libraryTable.js')
